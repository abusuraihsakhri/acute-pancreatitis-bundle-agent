version: '3.8'

services:
  acute-pancreatitis-bundle-engine:
    build: .
    container_name: acute-pancreatitis-bundle-engine
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=acute-pancreatitis-bundle-engine-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
